spring:
  config:
    activate:
      on-profile: test

  # Docker 컨테이너로 MySQL, Redis 서버 구동
  docker:
    compose:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    open-in-view: false
